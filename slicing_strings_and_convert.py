numbers = 12345678
convert = str(numbers)
print(f"I converted the numbers integer list which contains the following numbers:{numbers} in to a string list that still contains the same numbers: {convert}, but just that it is now a string")

substring = convert[0:4]
print(f' Now I have sliced these numbers:{convert} in to a substring, to get the following output: {substring}')